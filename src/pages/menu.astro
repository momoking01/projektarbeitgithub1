---
import Layout from "../components/layout.astro";
---

<Layout>
  <section class="menu-container">
    <h1>Our Menu</h1>

    <div class="menu-category">
      <h2>Pizzas</h2>
      <div class="menu-grid">
        <div class="menu-item">
          <span class="item-name">Pizza Margherita</span> 
          <button onclick="addToCart('Pizza Margherita')">Add to Cart</button>
        </div>
        <div class="menu-item">
          <span class="item-name">Pizza Pepperoni</span> 
          <button onclick="addToCart('Pizza Pepperoni')">Add to Cart</button>
        </div>
        <div class="menu-item">
          <span class="item-name">Pizza Quattro Stagioni</span> 
          <button onclick="addToCart('Pizza Quattro Stagioni')">Add to Cart</button>
        </div>
      </div>
    </div>

    <div class="menu-category admin-section">
      <h2>Admin Products</h2>
      <div class="menu-grid" id="admin-products-list"></div>
    </div>
  </section>
</Layout>

<script is:inline>
  function loadAdminProducts() {
    let products = JSON.parse(localStorage.getItem("products")) || [];
    let adminProductsList = document.getElementById("admin-products-list");
    adminProductsList.innerHTML = "";
    products.forEach(product => {
      let div = document.createElement("div");
      div.classList.add("menu-item", "admin-item"); // 💡 Klasse wird aktiv zugewiesen
      div.innerHTML = `<span class="admin-product-name">${product}</span> 
                       <button class="admin-button" onclick="addToCart('${product}')">Add to Cart</button>`;
      adminProductsList.appendChild(div);
    });
  }

  window.onload = loadAdminProducts;
</script>

<style>
  .item-name {
    font-weight: bold;
    font-size: 18px;
    color: white;
    display: block;
    margin-bottom: 10px;
  }

  .menu-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(255 255 255 / 15%);
    border-radius: 10px;
    padding: 20px;
    backdrop-filter: blur(5px);
    transition: transform 0.3s ease-in-out;
  }

  .menu-item:hover {
    transform: scale(1.05);
  }

  .admin-section {
    background: rgba(0 0 0 / 60%);
    padding: 30px;
    border-radius: 15px;
    margin-top: 30px;
  }
</style>
